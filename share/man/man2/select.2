NAME
  select - KNUCK syscall

SYNOPSIS
  select(reads?, writes?, timeout?) -> n, r, w

DESCRIPTION
  Wait for readiness on fd sets. Returns count and ready tables.

RETURN VALUES
  n, r, w

SEE ALSO
  poll

KNUCK(2) manual page. Part of the KNUCK kernel distribution.
